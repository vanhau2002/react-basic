import { Counter } from "../features/counter/Counter";
function AppToolkit() {
    return (
        <main className="AppToolkit" style={{ textAlign: 'center' }}>
            <Counter />
        </main>
    );
}

export default AppToolkit;